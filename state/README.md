# state

Ephemeral artifacts generated by automation scripts (e.g., `STATE_FILE` JSON manifests and the `results.db` SQLite score log). These files capture transient lab data and must not be committed.
